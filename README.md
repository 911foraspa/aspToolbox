# aspToolbox
azerbaycanca tool box! ingilis dili yaxinda gelecek
indiliy sadece fedora qurulum indexi var yaxinda windows linux ucunde destey gelecey

# Fedora/Linux
chmod +x run.sh:
./run.sh

#macOS
./runsh

# Windows
- if u have bash:
./run.sh

- if u have powershell:
  ./run.bat

- in cmd:
  run.bat
  
  # Termux
./run.sh
